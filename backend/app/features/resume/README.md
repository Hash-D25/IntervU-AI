# features/resume

Resume upload, parsing, and structured extraction.

Responsibility: turn an uploaded resume into structured data — skills, projects,
technologies, and experience — that later powers personalized questions.

Files (added iteratively): `router.py`, `service.py`, `repository.py`,
`models.py`, `dto.py`, `tests/`.
