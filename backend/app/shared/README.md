# app/shared

Framework-agnostic building blocks reused across features.

Examples: common value objects, result/error types, small pure utilities.

Keep this small. Anything specific to one feature belongs in that feature, not here.
