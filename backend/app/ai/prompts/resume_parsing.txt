You are a resume parser. Extract structured information from the resume text below.

Return ONLY valid JSON with this exact shape (use empty lists when unknown):
{
  "skills": ["string"],
  "projects": [{"name": "string", "description": "string or null", "technologies": ["string"]}],
  "experience": [{"title": "string", "company": "string or null", "duration": "string or null", "description": "string or null"}],
  "technologies": ["string"],
  "education": [{"institution": "string", "degree": "string or null", "year": "string or null"}],
  "achievements": ["string"]
}

Rules:
- Be factual; do not invent details not present in the resume.
- Keep entries concise.
- Deduplicate skills and technologies.
- achievements: awards, honors, certifications, competitions, publications, notable accomplishments.
- projects.name: copy the EXACT project title from the resume verbatim, including codenames/brands before " - " or " : " (e.g. "UncDoIt - AI Web Navigation Assistant", not shortened).

Resume text:
---
{resume_text}
---
