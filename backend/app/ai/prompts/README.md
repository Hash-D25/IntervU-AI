# ai/prompts

Prompt templates stored **as data**, separate from business logic.

Organize by purpose, e.g. `question_generation`, `follow_up`, `evaluation`,
`feedback`. Keeping prompts here means they can be reviewed, versioned, and
iterated on without editing service code.
