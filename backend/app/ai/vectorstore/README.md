# ai/vectorstore

ChromaDB wrapper for semantic retrieval.

Responsibility: index and query resume content and job descriptions so questions
can be grounded in the candidate's actual background. Exposes a small interface
so the underlying store can be replaced.
