# src/stores

Zustand stores for **explicit client state** (e.g. interview flow UI state).

Keep server data out of here - that belongs to React Query. Stores hold only
ephemeral/local UI state that the client owns.
