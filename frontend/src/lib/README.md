# src/lib

Cross-cutting client infrastructure.

Planned contents:

- `api-client.ts` - typed fetch wrapper around `NEXT_PUBLIC_API_BASE_URL`.
- `query-client.ts` - React Query client + provider setup.

This is the single place server communication is configured.
