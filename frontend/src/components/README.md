# src/components

Shared, reusable presentational components (buttons, inputs, layout primitives).

No feature-specific logic here — anything tied to one capability belongs in that
feature under `src/features`.
