# src/app

Next.js App Router routes: layouts, pages, and route handlers.

Keep pages thin — they compose feature modules from `src/features` and shared
components from `src/components`. Data fetching goes through `src/lib`.
